<?xml version="1.0" encoding="utf-8"?>
<!--
  Shown from the moment the launch window opens until the React root view
  draws. AppTheme had no windowBackground, so that gap was a plain white
  screen — the "empty white screen" on cold start.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
    <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/splash_logo" />
    </item>
</layer-list>
