<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!--
      Google Play in-app purchases (membership, F17). The Play Billing Library
      also merges this permission and writes com.google.android.play.billingclient.version
      so Console does not treat the app as the deprecated AIDL billing API.
      Receipts are verified by POST /billing/verify-purchase.
    -->
    <uses-permission android:name="com.android.vending.BILLING" />

    <!--
      Push notifications. Required from Android 13 (API 33) on, and requested at
      runtime — declaring it alone shows nothing. Without it FCM delivers the
      message and the OS drops it silently, which looks identical to not being
      sent. See registerPushToken() in src/lib/push.ts.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:networkSecurityConfig="@xml/network_security_config"
      android:supportsRtl="true">
      <!-- TODO: Replace with your Google Maps API key from https://console.cloud.google.com -->
      <meta-data
        android:name="com.google.android.geo.API_KEY"
        android:value="YOUR_GOOGLE_MAPS_API_KEY" />

      <!--
        Channel every push lands in unless the payload names another. Declared
        here so a notification arriving while the app is killed still has a
        channel: FCM creates its own unnamed one otherwise, which shows up in
        Android settings as "Miscellaneous" and cannot be relabelled later.
      -->
      <!--
        tools:replace because react-native-firebase_messaging declares this same
        meta-data with an empty value; without it the manifest merger fails
        rather than picking a winner.
      -->
      <meta-data
        android:name="com.google.firebase.messaging.default_notification_channel_id"
        android:value="@string/default_notification_channel_id"
        tools:replace="android:value" />
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- Supabase OAuth redirect. Without this the browser finishes the
             Google exchange and has nowhere to hand the session back to. -->
        <intent-filter android:autoVerify="false">
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="mehram" android:host="auth-callback" />
        </intent-filter>
      </activity>
    </application>
</manifest>
